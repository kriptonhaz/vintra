---
"@vintra/web": patch
---

Hero section: add "Tampilkan judul" and "Tampilkan alamat" toggles so tenants whose hero photo already contains its own text can hide the auto-overlaid business name + address instead of having them stamped on top. Defaults stay on, so existing sites are unchanged. (Heading and tagline copy were already editable.)
