---
"@vintra/web": patch
---

POS dashboard: add an "Item Terjual Hari Ini" stat card to the top row showing total quantity of items sold today (sum across all products). Stat row now wraps as 2 columns on `sm` and 4 on `lg`.
