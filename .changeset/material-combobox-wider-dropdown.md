---
"@vintra/web": patch
"@vintra/db": patch
"@vintra/shared": patch
---

`<MaterialCombobox>` dropdown panel no longer collapses to the narrow item/bahan column width — it now opens at a fixed 24rem (~384px), capped at viewport-2rem on mobile. Material names and `@ Rp x/unit` suffixes fit without truncation on the Hitung HPP calculator table.
