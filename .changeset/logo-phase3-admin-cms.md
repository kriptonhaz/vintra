---
"@vintra/web": minor
---

Logo AI Phase 3 — admin CMS. New `/admin/logo` page mirrors `/admin/konten` (template editor + fields/options CRUD), with an added "Field Type" picker so admins can declare a field as a free-text input (e.g. business name, tagline) instead of a dropdown. Admin sidebar gets a "Logo AI" entry under Modules.
