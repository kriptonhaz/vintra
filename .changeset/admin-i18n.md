---
"@vintra/web": patch
---

i18n the admin panel, sidebar Pengaturan section, impersonation banner, and tenant `/settings/members` page. All hardcoded strings in admin/* routes and new layout components now go through t() with matching id/en locale entries.
